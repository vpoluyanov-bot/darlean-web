# Homepage media

Everything here is generated by `scripts/encode-media.mjs` from the original
Claude Design export — nothing in this folder is hand-edited. To rebuild it:

```sh
npm run media -- "/path/to/Darvis export"
```

The sources could not be pulled through the design sync tool, which truncates
any binary over 256 KB, so the export has to be on disk.

## What ships

Each clip exists twice. The browser takes the first line it can decode:

- **`.webm` — AV1.** The one almost everyone gets.
- **`.mp4` — H.264.** The fallback for Safari before 17.4 and older hardware.

Plus a `-poster.webp` still, shown before the clip has loaded. The agent clips
use their own portrait as the poster, so they have no separate poster file.

Agent portraits ship as `.webp` with a `.png` fallback, wired through
`<picture>`.

None of it carries an audio track; the sources had none either.

## Sizes

| Clip | Source | AV1 | H.264 |
|---|---|---|---|
| `hero-main` | 3.63M | 0.44M | 0.79M |
| `hero-notifications` | 1.77M | 0.28M | 0.42M |
| `feature-projects` | 3.45M | 0.36M | 0.67M |
| `feature-requests` | 1.21M | 0.15M | 0.28M |
| `feature-org-chart` | 0.85M | 0.10M | 0.20M |
| `feature-meetings` | 1.18M | 0.20M | 0.29M |
| `agent-task` | 11.30M | 0.59M | 1.50M |
| `agent-report` | 11.27M | 0.77M | 1.57M |
| `agent-expense` | 11.48M | 0.59M | 1.41M |
| `agent-secretary` | 11.30M | 0.59M | 1.48M |
| portraits (4) | 3.26M | 0.06M webp | 0.75M png |

60.7M of sources become 13.6M on disk. A visitor who scrolls the whole page
downloads one encoding, so about 4.2M on AV1 or 9.4M on H.264 — and only
0.7M of that before the first scroll.

## Where each source came from

| Output | Source in the design export |
|---|---|
| `hero-main` | `assets/hero-main-v2.mp4` |
| `hero-notifications` | `assets/hero-notifications-v2.mp4` |
| `feature-projects` | `uploads/Раскрытие проекта (1).mp4` |
| `feature-requests` | `uploads/Процесс заявки (1).mp4` |
| `feature-org-chart` | `uploads/Оргструктура анимация (1).mp4` |
| `feature-meetings` | `uploads/Meeting Flow (1).mp4` |
| `agent-task` | `assets/agent-task.mp4` |
| `agent-report` | `assets/agent-report.mp4` |
| `agent-expense` | `assets/agent-expense.mp4` |
| `agent-secretary` | `assets/agent-secretary.mp4` |
| `agent-task.png/.webp` | `assets/agent-task.png` |
| `agent-report.png/.webp` | `assets/_report_frame.png` |
| `agent-expense.png/.webp` | `assets/_expense_frame.png` |
| `agent-secretary.png/.webp` | `assets/_secretary_frame.png` |
| `ai-sphere.json` | `assets/ai-sphere.json` (copied as-is) |

## The Lottie sphere

`ai-sphere.json` drives the sphere behind the AI headline, played by the
vendored `public/vendor/lottie_light.min.js`. Both are fetched only when that
section comes within a screen, so neither touches first paint.

It is still the heaviest thing on the page by a wide margin: **3.1 MB gzipped**,
against 4.2 MB for every other clip on the page put together. The file is not
really vector art — it is 300 PNG frames at 160x160 encoded as base64 inside
the JSON, which is why it compresses so badly. See the note in CLAUDE.md.
