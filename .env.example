# Analytics measurement IDs.
#
# Both have defaults in src/config.ts, so a normal build needs nothing here.
# Copy this file to .env to point a staging build at different properties, or
# set either to an empty string to drop that tag from the build entirely.

PUBLIC_GA4_ID=G-P04TEGPQQ1
PUBLIC_GTM_ID=GTM-5L6MW4QX
